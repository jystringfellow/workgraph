package facts

import "testing"

func TestGitHubCaptureStoresPullRequestEvent(t *testing.T) {
	t.Skip("TBD: github capture stores pull request events with repository, number, URL, state, actor, and title")
}

func TestGitHubCaptureLinksProjectByLocalRemote(t *testing.T) {
	t.Skip("TBD: github activity for a repository remote is associated with the matching local project")
}

func TestGitHubCaptureLinksProjectByCommitSHA(t *testing.T) {
	t.Skip("TBD: github activity referencing a captured commit is associated with that commit's local project")
}

func TestGitHubCaptureStoresIssueEvent(t *testing.T) {
	t.Skip("TBD: github capture stores issue events with repository, number, URL, state, actor, and title")
}
